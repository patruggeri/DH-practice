// Probando .replace()
function reemplazoFastFast(texto, aReemplazar, reemplazo) {
    return texto.replace(aReemplazar, reemplazo);
};

console.log(reemplazoFastFast("Cuando era chico, vivía en Italia", "Italia", "España"));
